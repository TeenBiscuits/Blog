---
import type { HTMLAttributes } from 'astro/types'
import { type Lang } from '@/i18n'

type Props = HTMLAttributes<'a'>

const locale = Astro.currentLocale as Lang
const { href, class: className, ...props } = Astro.props

const { pathname } = Astro.url
const isActive =
	href === pathname ||
	href === pathname.replace(/\/$/, '') ||
	(pathname.startsWith(`/${locale}/posts`) && href === `/${locale}/posts`)
---

<a href={href} class:list={[className, { active: isActive }]} {...props}>
	<slot />
</a>
<style>
	@reference "@/styles/global.css";

	a {
		@apply decoration-wavy underline-offset-4 hover:underline relative decoration-2 decoration-accent dark:decoration-accent-dark;
	}
	a.active {
		@apply underline decoration-accent dark:decoration-accent-dark;
	}
</style>
